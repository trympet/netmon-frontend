import { RowExpandDirective } from './row-expand.directive';

describe('RowExpandDirective', () => {
  it('should create an instance', () => {
    const directive = new RowExpandDirective();
    expect(directive).toBeTruthy();
  });
});
